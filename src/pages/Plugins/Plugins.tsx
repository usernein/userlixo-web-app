import { FC } from "react";
import styles from "./Plugins.module.scss";

interface PluginsProps {}

const Plugins: FC<PluginsProps> = () => (
  <div className={styles.Plugins}>Nothing here yet.</div>
);

export default Plugins;
