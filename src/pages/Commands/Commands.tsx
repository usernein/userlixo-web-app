import { FC } from "react";
import styles from "./Commands.module.scss";

interface CommandsProps {}

const Commands: FC<CommandsProps> = () => (
  <div className={styles.Commands}>Nothing here yet.</div>
);

export default Commands;
