import { FC } from "react";
import styles from "./Settings.module.scss";

interface SettingsProps {}

const Settings: FC<SettingsProps> = () => (
  <div className={styles.Settings}>Nothing here yet.</div>
);

export default Settings;
